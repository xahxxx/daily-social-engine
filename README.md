# Hunk Mao Daily Social Engine

This version is built for strict current-event filtering in these categories only:
Science and technology, AI, pets and animals, space, worldly/weird news, cryptocurrency.

Default daily workflow is safe: it generates artifacts and validates caption/hashtags, but does not publish unless `publish_to_instagram` is set to `true` when manually running the workflow.
